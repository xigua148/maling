# 安全政策（Security Policy）

码铃（MaLing）是一个**本地优先**的 Windows 桌面应用：它会在你的电脑上读写文件、执行命令、
看屏与屏幕操作，并使用**你自己的**大模型 API Key。我们非常重视这类能力带来的安全责任。

---

## 一、支持范围

| 版本 | 是否接收安全修复 |
|---|---|
| 最新发布版（见 [Releases](https://github.com/xigua148/maling/releases)） | ✅ 是 |
| 上一大版本 | ⚠️ 视严重程度而定 |
| 更早版本 | ❌ 否（请先升级到最新版） |

---

## 二、如何报告漏洞

**请勿用公开 Issue 报告安全问题**（公开即等于把利用方法告诉所有人）。

优先使用 GitHub 的**私密漏洞报告**：

> 仓库页面 → **`Security`** 标签页 → **`Report a vulnerability`**
> （直达：https://github.com/xigua148/maling/security/advisories/new ）

报告时请尽量包含：

- 受影响的版本 / 提交号；
- 复现步骤（越具体越好，最好附最小复现）；
- 影响范围（例如：可越权写文件 / 可绕过授权确认 / 可泄漏 API Key）；
- 你期望的正确行为；
- （可选）你的修复建议。

我们会在 **7 天内**首次回复，并在修复发布后于 Release Notes 中致谢（如果你愿意署名）。

---

## 三、安全设计要点（供参考，便于你判断某行为是否属于漏洞）

- **本地优先**：配置、会话、记忆、日记等数据都保存在本机用户目录，不上传第三方服务器
  （除你自己配置的模型 API 之外）。
- **API Key 只存本地**：`config.yaml` 位于本机用户目录，且已被 `.gitignore` 排除，**不会进入仓库**。
- **危险操作需确认**：修改类工具在**工作区目录外**会弹窗询问「仅这一次 / 拒绝」；
  无确认渠道时**一律拒绝**（fail-safe，绝不默认放行）。
- **命令白名单**：`run_command` 受命令与语法白名单约束（如 `pip` 与任意 shell 一律拒绝）。
- **路径守卫**：核心层有路径校验，拒绝越出允许范围的写入。
- **看屏 / 屏幕操作**：均需你显式开启；屏幕操作**每一步**都先弹确认；低置信度时先问再动。
- **自动更新**：下载后强制 **SHA-256 校验**，安装需你手动确认，失败自动回滚，且不影响用户数据。

---

## 四、已知边界（不是漏洞，但请知悉）

- 本应用会调用**你配置的第三方模型 API**，发送给它的内容受该服务商的隐私政策约束。
- 「免提语音」依赖外部语音识别服务（Google STT），需要联网。
- 你自己在本机执行的命令、或你主动授权的屏幕操作，其后果由你自行承担。
- 若你把 `config.yaml`（含 API Key）手动分享给他人，风险自负——请勿分享。

---

## 五、致谢

感谢每一位负责任地披露问题的研究者。修复发布后，我们会在 Release Notes 中致谢（除非你希望匿名）。
