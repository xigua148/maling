/* v2.1(UI-Fix-0912): 3 个页面的 setStyleSheet 只覆盖容器选择器,内部 QLabel 缺省色导致全页文字看不见。加全局规则兜底,更具体的 ID 选择器按特异性胜出。*/
QLabel { color: ${text}; }

/* ============================================================
   base.qss —— 码铃「简洁 × 女仆基调」(B8/D7) 公共组件结构层

   渲染链（theme_engine.load_theme）：
     qss = base.qss（本文件，公共结构/布局/间距/圆角/密度）
          + cute|minimal|maid.qss（配色/肤感层，后加载可覆盖）
   变量：颜色与 layout token（形如 bg / spacing_md / radius_lg）在 load_theme
   统一替换为当前主题的值；font_family 同理。

   v1.2 范围（design R1 分两段执行）：
     - 本文件只覆盖「本批新增/重构 UI 组件」；全部选择器走 objectName 命名空间，
       避免与既有三套 QSS 的通用选择器互相干扰，保证三主题零回归。
     - 既有三套 QSS 的「收窄为肤感层」全量裁剪 → P2（需 U-5 offscreen 截图回归兜底）。

   B8 对照：远看简洁（扁平、克制描边、清晰状态区分）；近看码铃（玫粉/奶油白
   由主题 primary 变量透出、柔和倒角走 radius、留白走 spacing token）。
   ============================================================ */

/* ---------- 1. B9「模型与接口」专区（ModelConfigPanel） ---------- */

/* 面板根：放进设置分区（settingsSection 已是卡片底），自身透明 */
QWidget#modelConfigPanel {
    background: transparent;
}

/* 分区说明小字（标题下引导文案） */
QLabel#modelSectionDesc {
    color: ${text_hint};
    font-size: 12px;
    background: transparent;
}

/* ① 状态头卡片 */
QFrame#modelStatusCard {
    background-color: ${bg_light};
    border: 1px solid ${divider};
    border-radius: ${radius_md};
    padding: ${spacing_md};
}
QLabel#modelStatusProvider {
    font-size: 15px;
    font-weight: bold;
    color: ${text};
    background: transparent;
}
QLabel#modelStatusStateText {
    font-size: 12px;
    color: ${text_secondary};
    background: transparent;
}
QLabel#modelStatusMeta {
    font-size: 12px;
    color: ${text_secondary};
    background: transparent;
}
/* Key 末四位脱敏展示（chip） */
QLabel#modelStatusKeyChip {
    background-color: ${bg_card};
    border: 1px solid ${divider};
    border-radius: ${radius_sm};
    color: ${text};
    font-size: 11px;
    padding: 2px 8px;
}
/* 缺 Key 引导提示（默认隐藏，缺 Key 时置 Visible） */
QLabel#modelMissingKeyHint {
    color: ${state_warn};
    font-size: 12px;
    font-weight: bold;
    background: transparent;
}

/* ② 厂商卡片点选区（替代纯下拉，B9④） */
QPushButton#providerCard,
QPushButton#providerCardSelected {
    background-color: ${bg_card};
    border: 1px solid ${divider};
    border-radius: ${radius_md};
    padding: ${spacing_sm};
    font-weight: normal;
    font-size: 12px;
    color: ${text};
    text-align: left;
}
QPushButton#providerCard:hover {
    background-color: ${surface_muted};
    border-color: ${focus_accent};
}
QPushButton#providerCard:pressed {
    background-color: ${bg_light};
}
/* 选中态：码铃粉描边 = 保留主色辨识度 */
QPushButton#providerCardSelected {
    border: 2px solid ${primary_dark};
    background-color: ${bg_light};
}
QPushButton#providerCardSelected:hover {
    background-color: ${surface_muted};
    border-color: ${primary_dark};
}
/* 卡片内文本（QLabel 默认不透明度，需显式上色） */
QLabel#providerCardTitle {
    color: ${text};
    font-size: 13px;
    font-weight: bold;
    background: transparent;
}
QLabel#providerCardDesc {
    color: ${text_secondary};
    font-size: 11px;
    background: transparent;
}
QLabel#providerCardModel {
    color: ${text_hint};
    font-size: 11px;
    font-family: ${font_family};
    background: transparent;
}
/* 「本地」小标签（ollama 等本地运行） */
QLabel#providerCardTagLocal {
    color: ${primary_dark};
    background-color: ${bg_light};
    border: 1px solid ${primary};
    border-radius: ${radius_pill};
    font-size: 10px;
    padding: 1px 6px;
}

/* ③ 编辑区：Key / Base URL / 模型 输入 */
QWidget#modelEditArea {
    background: transparent;
}
QLabel#modelFieldLabel {
    color: ${text_secondary};
    font-size: 12px;
    background: transparent;
}
/* 输入框/下拉继承主题肤感层的通用 QLineEdit/QComboBox 样式；
   这里只补更舒适的纵向密度与圆角（id 优先级高于主题通用类型选择器） */
QLineEdit#modelKeyEdit,
QLineEdit#modelUrlEdit {
    padding: ${spacing_sm} ${spacing_md};
    border-radius: ${radius_md};
}
QComboBox#modelModelCombo {
    padding: ${spacing_sm} ${spacing_md};
    border-radius: ${radius_md};
}
/* ① 下拉右侧下三角指示（叠加 QLabel，颜色随主题；落在 drop-down 区域，点击透传给 combo） */
QLabel#modelComboArrow {
    color: ${text_secondary};
    background: transparent;
    font-size: 14px;
    font-weight: bold;
}
QPushButton#modelShowKeyBtn {
    background: transparent;
    color: ${text_hint};
    border: none;
    font-size: 11px;
    padding: 2px 4px;
}
QPushButton#modelShowKeyBtn:hover {
    color: ${primary_dark};
    text-decoration: underline;
}
/* 本地厂商（ollama）说明小字 */
QLabel#modelLocalHint {
    color: ${state_ok};
    font-size: 11px;
    background: transparent;
}
/* 保存主按钮：略为加宽，保持「主操作」位阶 */
QPushButton#modelSaveBtn {
    min-width: 108px;
    padding: ${spacing_sm} ${spacing_lg};
}

/* ---------- 1b. 次级 / 卡片外按钮：幽灵底（accent 图标方可见） ----------
   v2.1 真机留档发现 A：`#secondaryBtn` 与页面层 `#memoryBookBtn` 此前**从未**被
   任何 QSS 定义过 → 落到通用 `QPushButton` 实底；而装饰助手 `_decorate_button`
   给图标取色 accent，accent 与 primary 在多套主题里同值（ui_night/ui_whale 两者
   皆同色）→ 图标色 == 按钮底色，图标**完全不可见**（非豆腐块，静态扫描抓不到）。
   本段补全局定义；base.qss 对全部主题生效，单点收口，避免逐主题漂移。
   注意：本段文字色给 `accent_text`（「文字用」强调色）而非 accent —— 页面层
   #memoryBookBtn 是文字型次级按钮，落浅色底时 accent 仅 2.1~3.1:1。卡片内实例
   仍由 page_memory_book 的**控件级内联样式表**覆盖（优先级更高），不受本段影响。
   （本条 v2.2.1 订正：原文写 accent_text 而实现留 ${accent}，二者不符。）
   本段**刻意不设** padding / font-size / font-weight —— 避免改变按钮尺寸引发版式位移。 */
QPushButton#secondaryBtn,
QPushButton#memoryBookBtn {
    background: transparent;
    color: ${accent_text};
    border: 1px solid ${divider};
    border-radius: ${radius_md};
}
QPushButton#secondaryBtn:hover,
QPushButton#memoryBookBtn:hover {
    background-color: ${surface_muted};
    border-color: ${accent};
}
QPushButton#secondaryBtn:pressed,
QPushButton#memoryBookBtn:pressed {
    background-color: ${bg_light};
    /* v2.2.1 订正：本伪态换底为 ${bg_light} 却未给 color → 单独命中时回落常态的
       ${accent_text}，落 ${bg_light} 上 = 4.125 / 4.397 / 5.335 / 4.280（三套浅色 <4.5）。
       取 ${text}（vs ${bg_light} = 14.492 / 11.556 / 12.503 / 11.980，四套全 ≥4.5）——
       与同文件「12. 聊天气泡动作行」的 #actionBtn:pressed 同款手法与同一对数。 */
    color: ${text};
}
QPushButton#secondaryBtn:disabled,
QPushButton#memoryBookBtn:disabled {
    background-color: ${disabled_bg};
    color: ${disabled_text};
    border-color: ${divider};
}

/* ---------- 1c. 角色页侧栏按钮（220px 窄栏 + 固定 28px 高） ----------
   v2.1 真机留档发现 B：角色页新建工具区四个按钮（新建角色 / 用预设新建 /
   导出角色卡 / 导入角色卡）都 setFixedHeight(28)，而肤感层通用 QPushButton 的
   padding: 8px 20px 上下吃掉 16px，只剩 12px 装 13px 的字 → 文字被上下切掉
   （用户报「字显示不全」）。三个 #secondaryBtn 未设 padding，继承同一份值，故四个一起中招。
   本段把 roleSidebar 作用域内按钮的垂直 padding 收到 2px、水平收到 8px，
   兼顾 220px 窄栏与 28px 定高。
   特异性 (1,0,2) > QPushButton#secondaryBtn (1,0,1)，能覆盖继承来的 padding；
   #secondaryBtn:hover / :disabled 均为 (1,1,1) 但未设 padding，不受影响。 */
QWidget#roleSidebar QPushButton {
    padding: 2px 8px;
}

/* ---------- 1d. 设置页「自定义… / 恢复默认」按钮（定高 28px） ----------
   v2.1(UI-Fix-0914)：与 §1c 同一类问题 —— 定高 28px 遇上肤感层通用 QPushButton 的
   padding: 8px 20px，上下吃掉 16px，只剩 12px 装 13px 的字 → 文字被上下切掉
   （用户报「字显示不全 / 字体不符 / 方框是方形」）。
   本段把设置页分区内的次级按钮垂直 padding 收到 2px、水平 10px，兼顾定高与可读性。
   特异性 (1,1,2) > QPushButton#secondaryBtn (1,0,1)，可覆盖继承来的 padding。 */
QWidget#settingsSection QPushButton#secondaryBtn {
    padding: 2px 10px;
}

/* ---------- 1e. 设置页「外观与效果」控件密度与圆角 ----------
   这四个下拉框不应依赖各肤感层的通用 QComboBox：通用规则的 radius_sm、
   6px 上下 padding 在不同字体度量下会显得生硬，且与 28px 定高操作按钮不协调。
   此处只命名空间化覆盖设置页外观区，字体仍取全局 ${font_family}，换字体后随
   ThemeEngine 重建 QSS 一并刷新；不影响模型、更新等其它下拉控件。 */
QWidget#settingsSection QComboBox#settingsThemeCombo,
QWidget#settingsSection QComboBox#settingsAppearanceCombo,
QWidget#settingsSection QComboBox#settingsFontCombo,
QWidget#settingsSection QComboBox#settingsAnimationCombo {
    background-color: ${bg_card};
    color: ${text};
    border: 1px solid ${border};
    border-radius: ${radius_md};
    min-height: 32px;
    padding: 2px 10px;
    font-family: ${font_family};
    font-size: 13px;
}
QWidget#settingsSection QComboBox#settingsThemeCombo:focus,
QWidget#settingsSection QComboBox#settingsAppearanceCombo:focus,
QWidget#settingsSection QComboBox#settingsFontCombo:focus,
QWidget#settingsSection QComboBox#settingsAnimationCombo:focus {
    border-color: ${accent_text};
}
QWidget#settingsSection QComboBox#settingsThemeCombo::drop-down,
QWidget#settingsSection QComboBox#settingsAppearanceCombo::drop-down,
QWidget#settingsSection QComboBox#settingsFontCombo::drop-down,
QWidget#settingsSection QComboBox#settingsAnimationCombo::drop-down {
    border: none;
    width: 26px;
}
QWidget#settingsSection QComboBox#settingsThemeCombo QAbstractItemView,
QWidget#settingsSection QComboBox#settingsAppearanceCombo QAbstractItemView,
QWidget#settingsSection QComboBox#settingsFontCombo QAbstractItemView,
QWidget#settingsSection QComboBox#settingsAnimationCombo QAbstractItemView {
    background-color: ${bg_card};
    color: ${text};
    border: 1px solid ${border};
    border-radius: ${radius_md};
    padding: 2px;
    font-family: ${font_family};
}
QWidget#settingsSection QToolButton#settingsStyleSwatch,
QWidget#settingsSection QToolButton#settingsAccentSwatch {
    border-radius: ${radius_sm};
}

/* ---------- 2. 首页「模型设置」快捷入口（U-4 最小入口） ---------- */

QPushButton#homeModelEntry {
    background-color: ${bg_light};
    border: 1px solid ${divider};
    border-radius: ${radius_md};
    padding: ${spacing_sm} ${spacing_md};
    color: ${text};
    font-size: 13px;
    font-weight: normal;
    text-align: left;
}
QPushButton#homeModelEntry:hover {
    background-color: ${surface_muted};
    border-color: ${focus_accent};
    color: ${text};
}
QPushButton#homeModelEntry:pressed {
    background-color: ${bg_card};
}
QLabel#homeModelEntryDesc {
    color: ${text_hint};
    font-size: 11px;
    background: transparent;
}

/* ---------- 3. 侧栏「模型状态」直达按钮（U-4，含脱敏状态） ---------- */

/* 放置于帮助/关于之上的一整行状态入口；肤感上比导航项更轻 */
QPushButton#sidebarModelBtn {
    /* v2.1(UI): 状态入口而非主操作 —— 去掉永久底色 + 边框，与侧栏 nav 项保持平面统一。
       视觉减负：仅在 hover/pressed 给出微弱背景提示「可点」；focus 矩形保留（键盘可达性）。
       此前该按钮是侧栏里唯一带 ${bg_light} 填充 + ${divider} 边框的控件，粉色「框框」即由此而来。 */
    background-color: transparent;
    border: none;
    border-radius: ${radius_md};
    color: ${text};
    font-size: 12px;
    font-weight: normal;
    padding: 6px 8px;
    text-align: left;
}
QPushButton#sidebarModelBtn:hover {
    background-color: ${surface_muted};
    color: ${text};
}
QPushButton#sidebarModelBtn:pressed {
    background-color: ${bg_card};
}
/* 状态点（形状；颜色由代码用 theme_color 的 state_ok/state_warn 设入，禁止裸值） */
QLabel#sidebarModelDot {
    min-width: 8px;
    max-width: 8px;
    min-height: 8px;
    max-height: 8px;
    border-radius: 4px;
}

/* ---------- v1.9 B/D-V19-05：标题位字体（${font_title}） ----------
   标题 / 卡片标题 / 空态标题走标题字体（默认与正文同链；用户选「jf open 粉圆」
   时仅这些标题位换成粉圆，正文保持资源圆体 —— Q-E4「粉圆仅标题」）。
   ID 选择器特异性高于全局 QWidget 规则，故此处可覆盖全局正文族。 */
QLabel#toggleCardTitle,
QLabel#statusCardTitle,
QLabel#memoryBookTitle,
QLabel#miniGamesTitle,
QLabel#providerCardTitle {
    font-family: ${font_title};
}

/* v2.1：空态标题字号 —— 代码侧 setPointSize(16) 会被全局 font-size:14px 覆盖，
   故字号必须由 QSS 声明（单列一条，避免波及其余四个标题） */
QLabel#editorEmptyTitle {
    font-family: ${font_title};
    font-size: 16px;
}

/* ============================================================
   v2.1 S 组（V21-14）：公共控件精修 —— 毛玻璃根透明 / 五态补齐 /
   通用控件（滚动条·下拉·复选·滑块）/ 浮层分层
   ------------------------------------------------------------
   纪律（design-v21 §6 / D-V21-08）：
     · 只引用已注册的 token 占位符，本块新增规则无裸色十六进制字面量；
     · 不含过渡 / 动画样式声明（Qt 样式表不支持，一切动效归 gui/motion.py）；
     · 放置于 base（公共结构层）：下列选择器在四风格肤感层中均未定义，
       故此处规则对四风格一致生效（D-V21-09 正交化，无需分叉四份）。
   仅追加规则、不改动本文件既有规则的语义。
   ============================================================ */

/* ---------- 1. 毛玻璃根透明条件规则（D-V21-04 / §4.6 契约） ----------
   代码侧 _apply_glass_state：玻璃开 → 置 MainWindow 动态属性 glass="on"
   并 style().unpolish/polish 重刷；玻璃关 → 移除属性 → 恢复主题背景
   （R-Q③：行为与改动前一致）。属性名/值必须与本规则严格一致（glass / "on"）。
   卡片/气泡保留各自不透明底，材质只在留白/间隙透出（§6 共享知识第 8 条）。 */
QMainWindow[glass="on"],
QMainWindow[glass="on"] QWidget#glassCentralOuter,
QMainWindow[glass="on"] QSplitter#glassCentralSplitter,
QMainWindow[glass="on"] QStackedWidget#glassPageStack {
    background: transparent;
}
/*
   ⚠ 不使用「glass 主窗下所有后代都 transparent」的通配规则：页面根、侧栏、
   卡片、输入框与按钮仍需各自的主题底色保持可读。上面仅穿透
   MainWindow._setup_central_layout() 的三层结构容器；可见页面及内容控件不受影响。
*/

/* ---------- 2. 全局滚动条（竖向 + 横向，含全部子控件） ----------
   四风格肤感层仅定义 ChatWindow 局部滚动条，主区滚动条此前走原生样式；
   此处统一圆角/描边/悬停反馈（S-3①）。ChatWindow 局部规则特异性更高，不受影响。 */
QScrollBar:vertical {
    background: transparent;
    width: 10px;
    margin: 0;
}
QScrollBar::handle:vertical {
    background: ${border};
    border-radius: 5px;
    min-height: 28px;
}
QScrollBar::handle:vertical:hover {
    background: ${text_hint};
}
QScrollBar::handle:vertical:pressed {
    background: ${primary};
}
QScrollBar::add-line:vertical,
QScrollBar::sub-line:vertical {
    height: 0;
    background: transparent;
}
QScrollBar::add-page:vertical,
QScrollBar::sub-page:vertical {
    background: transparent;
}
QScrollBar:horizontal {
    background: transparent;
    height: 10px;
    margin: 0;
}
QScrollBar::handle:horizontal {
    background: ${border};
    border-radius: 5px;
    min-width: 28px;
}
QScrollBar::handle:horizontal:hover {
    background: ${text_hint};
}
QScrollBar::handle:horizontal:pressed {
    background: ${primary};
}
QScrollBar::add-line:horizontal,
QScrollBar::sub-line:horizontal {
    width: 0;
    background: transparent;
}
QScrollBar::add-page:horizontal,
QScrollBar::sub-page:horizontal {
    background: transparent;
}

/* ---------- 3. 输入框五态（normal/焦点由肤感层给；此处补 hover / disabled） ---------- */
QLineEdit:hover {
    border-color: ${focus_accent};
}
QLineEdit:disabled {
    background-color: ${disabled_bg};
    color: ${disabled_text};
    border-color: ${divider};
}

/* ---------- 4. 下拉框五态（补 hover / pressed(展开) / disabled；focus 由肤感层给） ---------- */
QComboBox:hover {
    border-color: ${focus_accent};
}
QComboBox:pressed,
QComboBox:on {
    border-color: ${primary};
}
QComboBox:disabled {
    background-color: ${disabled_bg};
    color: ${disabled_text};
    border-color: ${divider};
}

/* ---------- 5. 按钮 focus 态（键盘可达性，v2.1 上线收口） ----------
   此前此处为 `QPushButton:focus { outline: none; }` —— 全局移除焦点圈，键盘 Tab 到按钮时
   **零视觉反馈**（QSS 无 :focus-visible，无法只对鼠标点击隐藏）。本轮改为「保留一个可见但
   克制的焦点圈」：
     · 用 outline（不动 border / background / padding → 焦点切换零版式位移；且各 id 按钮规则
       均未声明 outline，故本规则对全部按钮生效，不会被 `QPushButton#id` 抢走）；
     · 取色 `${text}`：Qt 的焦点圈绘制在控件**内容区边缘**、压在按钮自身底色上，而四套新风格的
       focus_accent 与 primary 同值（实底主按钮上描边会被同色底吃掉，实测 diff=4px≈不可见）；
       `${text}` 是唯一在「primary 实底」与「bg_card / 透明底」两类按钮上都不退化的前景语义键。
     · 1px、无动画（QSS 天然静态 → off 档同样可见）。 */
QPushButton:focus {
    outline: 1px solid ${text};
}

/* ---------- 6. 复选框五态（normal/checked 由肤感层给；补 hover / focus / disabled） ---------- */
QCheckBox {
    padding: 2px 0;
}
QCheckBox::indicator:hover {
    border-color: ${focus_accent};
}
QCheckBox:focus,
QCheckBox::indicator:focus {
    border-color: ${accent_text};
}
QCheckBox::indicator:disabled {
    background-color: ${disabled_bg};
    border-color: ${divider};
}
QCheckBox:disabled {
    color: ${disabled_text};
}

/* ---------- 7. 滑块五态（normal 由肤感层给；补 hover / pressed / disabled） ---------- */
QSlider::handle:horizontal:hover {
    background: ${primary_dark};
}
QSlider::handle:horizontal:pressed {
    background: ${focus_accent};
}
QSlider::handle:horizontal:disabled {
    background: ${disabled_bg};
}
QSlider::sub-page:horizontal:disabled {
    background: ${disabled_bg};
}
QSlider::groove:horizontal:disabled {
    background: ${divider};
}

/* ---------- 8. 列表项五态（通用 QListWidget / QListView；具名列表特异性更高不受影响） ---------- */
QListWidget::item:hover,
QListView::item:hover {
    background-color: ${surface_muted};
}
QListWidget::item:selected,
QListView::item:selected {
    background-color: ${bg_light};
    color: ${primary_dark};
}
QListWidget::item:disabled,
QListView::item:disabled {
    color: ${disabled_text};
}

/* ---------- 9. 标签页补齐（本文件未定义；补 disabled / focus 可见） ---------- */
QTabBar::tab:disabled {
    color: ${disabled_text};
}
QTabBar::tab:selected:disabled {
    color: ${disabled_text};
}
QTabBar::tab:selected:focus {
    /* v2.1 上线收口：原用 ${focus_accent}，但四套新风格里 focus_accent ≡ primary，
       而选中页签的下边框本就是 ${primary} → 聚焦时**色值无变化**（实测 diff=0px）。
       改取 ${text}：与 primary 在所有主题下都可区分，且压在页签底（bg_card）上可辨。 */
    border-bottom: 2px solid ${text};
}

/* ---------- 9b. 焦点态补齐：列表 / 树视图 / 文本编辑 / 滑杆（键盘可达性，v2.1 上线收口） ----------
   背景：base §5 原全局隐藏按钮焦点圈；四套肤感层又对列表 / 树视图容器写 `outline: none`
   （移除原生点线焦点框）→ 键盘 Tab 无反馈；未命中任何 QSS 的裸 QTextEdit / QPlainTextEdit
   则走系统原生焦点框（实测是 Windows 蓝 #0067C0 = 「外来」样式），QPlainTextEdit 甚至干脆没有。
   QSS 无 :focus-visible，无法只对鼠标隐藏，故统一补回「可见但克制」的静态焦点态。
   手法（真机实测选取，非拍脑袋）：
     · 列表 / 树视图 / 文本编辑走 `border` —— `outline` 在 QAbstractScrollArea 上**不渲染**
       （实测 diff=0px），`border` 生效；且对文本编辑设 border 能**替换**系统原生蓝框
       （实测蓝像素 248 → 0）。具名容器（#sessionList / #projectTreeView）在肤感层写有更高
       特异性的 border/outline，故逐处用 id 规则补回。
     · 描边取色 `${accent_text}`（v2.2.1 收口）：v2.2.0 取 `${focus_accent}`，但四套新风格里
       focus_accent ≡ primary，浅色三套的描边压在页面底色上实测仅 **1.931 ~ 2.814**（<3:1，
       非文本图形不达标）；改取「文字用」强调色 `${accent_text}` 后四风格明暗实测 **≥4.125**，
       与 §5 按钮 / §9 页签改用 `${text}` 同属「同值/低对比退化即换键」的既有口径。
     · 无动画（QSS 天然静态，off 档同样可见）。
   注：已有具名焦点规则的编辑器（#chatInput / #codeEditorArea）特异性更高，不受本块影响。 */
QListWidget:focus,
QListView:focus,
QTreeView:focus {
    border: 1px solid ${accent_text};
}
QListWidget#sessionList:focus,
QTreeView#projectTreeView:focus {
    border: 1px solid ${accent_text};
}
QTextEdit:focus,
QPlainTextEdit:focus {
    border: 1px solid ${accent_text};
}
/* 滑杆：焦点落在手柄上 → 给手柄加一圈描边（不改变手柄尺寸/位置）；
   取色 ${text}（手柄本身即 ${primary} 填充，用 focus_accent 会被同色吃掉） */
QSlider::handle:horizontal:focus {
    border: 1px solid ${text};
}

/* ============================================================
   10. 浮层阴影分层（elevation 三档）
   ------------------------------------------------------------
   说明（诚实口径）：Qt QSS **不支持 box-shadow**，无法在样式表内绘制模糊投影；
   真正的模糊投影由代码侧 QGraphicsDropShadowEffect 承担（S-4 的既有落点，
   chat_window.py:106）。QSS 侧以「表面亮度 + 描边权重」表达三档层级，
   深色模式下底/边随 token 自动加深，层级由 ${border}/${divider} 描边保证可辨
   （D-2③：深色不依赖阴影，以描边补足）。
     E1 小：工具提示 / 内联浮标
     E2 中：菜单 / 下拉弹层
     E3 大：顶层对话框
   ============================================================ */
QToolTip {
    background-color: ${bg_card};
    color: ${text};
    border: 1px solid ${border};
    border-radius: ${radius_sm};
    padding: 4px 8px;
    font-size: 12px;
}
QMenu {
    background-color: ${bg_card};
    color: ${text};
    border: 1px solid ${border};
    border-radius: ${radius_md};
    padding: 4px;
}
QMenu::item {
    padding: 6px 24px 6px 12px;
    border-radius: ${radius_sm};
    background: transparent;
}
QMenu::item:selected {
    background-color: ${surface_muted};
    color: ${primary_dark};
}
QMenu::item:disabled {
    color: ${disabled_text};
}
QMenu::separator {
    height: 1px;
    background: ${divider};
    margin: 4px 8px;
}
QDialog {
    background-color: ${bg_card};
    border: 1px solid ${border};
}

/* ---------- 字号恢复（v2.1 后续） ----------
   背景：应用级 QSS 一旦声明 font-size，就会压过代码 setFont；各主题的
   `QWidget { font-size: 14px }` 因此把所有未命中的控件的代码字号抹平成 14px。
   id 选择器优先级高于类型选择器且与加载顺序无关，故集中放本文件单点修复。
   纪律：只设 font-size，不设 padding / font-weight / min-width 等会改变
   尺寸或重量的属性，避免推挤版式。
   【字号单一收口点】下列控件的字号只在本块定义；代码侧**不得**再对同一控件
   调用 setPointSize / setPixelSize —— 那会被这里压掉，只留下误导后人的死行。
   若确需调整，请改本块数值。 */
QLabel#pageTitle { font-size: 21px; }             /* 页面主标题 16pt */
QLabel#placeholderTitle { font-size: 24px; }      /* 占位页标题 18pt */
QLabel#onboardingSectionTitle { font-size: 21px; }/* 引导步骤内标题 16pt */
QLabel#onboardingStepTitle { font-size: 19px; }   /* 引导顶部步骤名 14pt */
QLabel#homeSectionTitle { font-size: 17px; }      /* 首页区块标题 13pt */
QLabel#helpSectionTitle { font-size: 17px; }      /* 帮助页区块标题 13pt */
QLabel#homeWelcome { font-size: 24px; }           /* 首页欢迎语 18pt */
QLabel#homeGreeting { font-size: 17px; }          /* 首页问候语 13pt */
QLabel#homeAvatarFallback { font-size: 61px; }    /* 头像兜底 emoji 46pt */
QLabel#featureCardIcon { font-size: 32px; }       /* 功能卡图标 24pt */
QLabel#authDialogTitle { font-size: 17px; }       /* 授权弹窗标题 13pt */
QLabel#themePreviewName { font-size: 17px; }      /* 风格卡名称 13pt */
QLabel#toolboxToolTitle { font-size: 19px; }      /* 工具箱工具名 14pt */
QLabel#statusCardValue { font-size: 17px; }       /* 状态卡数值 13pt */
QLabel#miniGamesTitle { font-size: 19px; }        /* 小游戏标题 14pt */
QLabel#pomPhase { font-size: 17px; }              /* 番茄钟阶段 13pt */
QLabel#pomTime { font-size: 59px; }               /* 番茄钟计时 44pt */
QLabel#sidebarTitle { font-size: 19px; }          /* 侧栏标题 14pt（工具箱/角色面板/计划编辑器） */
QLabel#statusCardIcon { font-size: 24px; }        /* 状态卡图标兜底 emoji 18pt */

/* ---------- 字号恢复 · 弹窗/分区标题补齐（v2.1 上线收口） ----------
   背景：以下 4 处标题的代码本意字号为 12pt(=16px)，其 `setPointSize` 在历史批次被作为
   「死字号」删除（确为死行：被全局 `QWidget{font-size:14px}` 压平）—— 删除无视觉回归，
   但层级随之丢失，现回落 14px 正文基线。本轮据「字号单一收口」纪律改由 QSS 单点补回。
   命名空间：`#updTitle` 自带 id，直接命中；其余三处的标题控件**无 objectName**（核实：以
   代码为准，评审给的 model_config_panel / kb_dialog / todo_dialog 是模块名而非 objectName），
   故沿用本项目既有「Python 类名选择器」惯用法（同 SidebarWidget / ChatWindow）作用域到各自
   容器；用 `>` 直接子选择器收窄，避免波及其他页面/兄弟控件（见各处验证读数）。 */
QLabel#updTitle { font-size: 16px; }          /* 更新下载弹窗标题（update_progress，12pt） */
ModelConfigPanel > QLabel { font-size: 16px; }/* 「模型与接口」面板节标题「选择厂商」（12pt） */
KnowledgeBaseDialog > QLabel { font-size: 16px; } /* 知识库弹窗标题（12pt） */
TodoDialog > QLabel { font-size: 16px; }      /* 待办弹窗标题（12pt） */

/* ---------- 10. 侧栏导航滚动条（v2.1 阶段 C-1：hover 门控 / WorkBuddy 风格） ----------
   用户诉求：默认隐藏，鼠标进入侧栏模块才显示（滚动中/激活时也显示）。
   实现：代码侧（sidebar.py）按光标是否在侧栏矩形内，把滚动条动态属性
   sidebarHover 置 True/False 并 unpolish/polish；本块依该属性切换 handle 颜色。
   · 恒占位：代码侧用 ScrollBarAlwaysOn，避免 AsNeeded 出现/消失导致横向跳版；
   · 细 / 圆角 / 无箭头 / 无底槽：与 ChatWindow 滚动条同款视觉语言（8px、4px 圆角）；
   · handle 取弱化色 ${text_hint}（悬停）/ ${primary}（直接拖拽 handle）。
   仅追加规则、不改动本文件既有规则的语义。 */
QListWidget#sidebarNavList QScrollBar:vertical {
    background: transparent;   /* 无底槽 */
    width: 8px;                /* 与 ChatWindow 滚动条同宽 */
    margin: 0;
}
QListWidget#sidebarNavList QScrollBar::handle:vertical {
    background: transparent;   /* 空闲：不可见 */
    border-radius: 4px;
    min-height: 30px;
}
QListWidget#sidebarNavList QScrollBar[sidebarHover="true"]::handle:vertical {
    background: ${text_hint};  /* 悬停 / 滚动中：弱化色 */
}
QListWidget#sidebarNavList QScrollBar[sidebarHover="true"]::handle:vertical:hover {
    background: ${primary};    /* 光标直接压住 handle：强调色 */
}
QListWidget#sidebarNavList QScrollBar::add-line:vertical,
QListWidget#sidebarNavList QScrollBar::sub-line:vertical {
    height: 0;                 /* 无箭头 */
    background: transparent;
}
QListWidget#sidebarNavList QScrollBar::add-page:vertical,
QListWidget#sidebarNavList QScrollBar::sub-page:vertical {
    background: transparent;   /* 无底槽 */
}

/* ---------- 11. 酒馆功能域控件（字号单一收口点，全部集中在本文件） ----------
   纪律：酒馆所有控件的字号 / 字重**只在此处定义**；`gui/widgets/tavern/*` 一律不写
   `font-size` / `font-weight`（`setPointSize/setPixelSize` 另有永久守卫测试）。
   **不设 color** —— 颜色一律由控件代码经 `theme_color(...)` 读时现取（§6.3 / §6.5），
   避免与代码取色争优先级。仅追加规则，不改动本文件既有规则的语义。
   11a = 世界书 / 我的故事 / 记录；11b = 叙述流 / 输入区 / 顶部 HUD。 */
/* --- 11a. 世界书 / 我的故事 / 记录 --- */
QLabel#tavernPanelTitle { font-size: 17px; font-weight: bold; }
QLabel#tavernPanelHint { font-size: 12px; }
QLabel#tavernEntryTitle { font-size: 14px; font-weight: bold; }
QLabel#tavernEntryKeys { font-size: 12px; }
QLabel#tavernEntrySummary { font-size: 12px; }
QLabel#tavernHitChip { font-size: 11px; }
QLabel#tavernEmptyHint { font-size: 12px; }
QLabel#tavernPlayTitle { font-size: 14px; font-weight: bold; }
QLabel#tavernPlayMeta { font-size: 12px; }
QLabel#tavernActiveChip { font-size: 11px; }
QLabel#tavernTraceRole { font-size: 13px; font-weight: bold; }
QLabel#tavernTraceField { font-size: 12px; }
QLabel#tavernTraceReason { font-size: 12px; }
QLabel#tavernEndingName { font-size: 12px; }
QPushButton#tavernPlayLoad { font-size: 12px; }
QPushButton#tavernPlayDelete { font-size: 12px; }

/* --- 11b. 叙述流 / 输入区 / 顶部 HUD --- */
QLabel#tavernNarrativeText { font-size: 15px; }
QLabel#tavernNarrativePlayer { font-size: 12px; }
QLabel#tavernNarrativeEmpty { font-size: 13px; }
QPushButton#tavernChoiceBtn { font-size: 13px; }
QPlainTextEdit#tavernInputEdit { font-size: 14px; }
QPushButton#tavernSendBtn { font-size: 14px; font-weight: bold; }
QLabel#tavernInlineHint { font-size: 12px; }
QLabel#tavernHudChapter { font-size: 15px; font-weight: bold; }
QLabel#tavernHudScene { font-size: 12px; }
QLabel#tavernHudItem { font-size: 11px; }
QLabel#tavernHudStatus { font-size: 12px; }

/* --- 11c. 酒馆页面（`gui/pages/page_tavern.py` 装配的页面级 id） ---
   同上纪律：本段**只给字号 / 字重**，不写 `color`（颜色由页面经 `theme_color` 读时现取）。
   11a / 11b 是控件侧 id，本段只收页面自建小部件的 id；上面两段一字未动。 */
QLabel#tavernCastName { font-size: 14px; font-weight: bold; }
QLabel#tavernCastRole { font-size: 11px; }
QLabel#tavernCastLine { font-size: 12px; }
QLabel#tavernSettingsLabel { font-size: 13px; }
QLabel#tavernSettingsHint { font-size: 12px; }
QCheckBox#tavernAllowPropose { font-size: 13px; }
QSpinBox#tavernBudgetSpin { font-size: 13px; }
QPushButton#tavernRerollBtn { font-size: 12px; }
QPushButton#tavernEditBtn { font-size: 12px; }
QLabel#tavernStartHint { font-size: 13px; }
QPushButton#tavernStartBtn { font-size: 14px; font-weight: bold; }

/* ---------- 12. 聊天气泡动作行（「复制」按钮 / 时间戳）换肤收口 ----------
   缺陷：`message_bubble.py` 的「复制」按钮（objectName=actionBtn）与时间戳
   （objectName=timeLabel）此前用**控件级内联样式表**写死裸粉 / 裸灰
   （#FFE4EC / #BBBBBB / #FFF0F3 / #FFB6C1 / #FF6B9D），而全仓 8 份 qss 中
   **没有任何** `QPushButton#actionBtn` / `QLabel#timeLabel` 规则 → 深色主题下
   这两个控件仍是浅粉 / 裸灰，与主题完全不搭（内联样式优先级高于 app 级 qss，
   故必须同时移除内联，单侧改动无效）。
   落点选择：放 base（公共结构层）——下列选择器在四风格肤感层中均未定义，
   符合 D-V21-09 正交化惯例（一处生效四风格，无需分叉四份）。

   取色纪律：
   · 常规态边框 ${border} / 文字 ${text_secondary}、悬停底 ${bg_light} /
     边框 ${accent_light}、时间戳 ${text_hint} —— 与同排的
     #readAloudBtn / #regenBtn（代码侧 theme_color 现取）保持同一语义键。
   · **悬停态文字色用 ${text}（「落在主题淡填充上的正文文字色」），不得用
     ${accent}/${primary}（「填充用」色），也不能用 ${text_on_accent}**：
     本规则 hover 的底 ${bg_light} 是一个**随主题翻转的淡填充**（浅色主题=浅底，
     ui_night=深底 #3A2430），故配它的文字键也必须随之翻转 —— ${text} 正是该键。
     实测候选令牌 vs ${bg_light} 的四风格对比度：
       ${text}            14.492 / 11.556 / 12.503 / 11.980  ← 唯一四套全 ≥4.5
       ${accent_text}      4.125 /  4.397 /  5.335 /  4.280  （三套浅色 <4.5）
       ${text_on_accent}  14.492 / 11.556 /  1.220 / 14.612  （ui_night 崩）
       ${accent} / ${primary_dark} / ${text_secondary} / ${text_hint}  全 < 3.9
     ⚠ ${text_on_accent} 的语义是「落在**强调实底**上的文字」，而 accent/primary
     实底在四套主题里**永远是亮色** → 该键永远取深色，落在 ui_night 的深色
     ${bg_light} 上只剩 1.220（不可读）。它**不是** ${bg_light} 的配对键。
   · 悬停/按下的**描边取 ${accent_text}，不取 ${accent_light} 也不取 ${accent}**：
     · ${accent_light} 不可用 —— 四套主题里 accent_light ≡ bg_light（逐字节相同），
       拿它当描边 → 描边与悬停底同色，对比度 1.000，**完全不可见**；
     · ${accent} 也不可用 —— 实测 ${accent} vs ${bg_light} = **2.783 / 1.931 / 5.317 /
       2.814**，三套浅色 **< 3:1**（ui_cream 仅 1.931），作为"看得见的描边"不达标。
       （注：3.144 / 2.538 / 3.853 / 3.841 是 **${primary_dark}** vs ${bg_light} 的读数，
       不是 ${accent} 的。）
     · 故取 ${accent_text} vs ${bg_light} = **4.125 / 4.397 / 5.335 / 4.280**，四套均
       ≥3:1（且 ≥4.5），既保住强调色相又真的看得见。此手法与 base §5/§9/§9b 一致 ——
       项目对「必须可见的装饰描边」本就取前景/文字类令牌（那里用的是 ${text}），
       而非填充类令牌。备选中性键 ${text_secondary} = 3.840 / 3.975 / 4.285 / 4.010。
   · :pressed / :disabled 复用既有令牌 ${bg_light} / ${disabled_bg} /
     ${disabled_text} / ${divider}，不引入未注册令牌。
   字号（11px / 10px）沿用原内联值，按「字号单一收口」纪律只在本块声明。 */
QPushButton#actionBtn {
    background: transparent;
    border: 1px solid ${border};
    border-radius: 6px;
    color: ${text_secondary};
    font-size: 11px;
    padding: 2px 8px;
}
QPushButton#actionBtn:hover {
    background: ${bg_light};
    border-color: ${accent_text};
    color: ${text};
}
/* 显式给 :pressed 声明 color：该伪态若单独命中（无 :hover 同时作用）而不给 color，
   会回落到常规态的 ${text_secondary} 落在 ${bg_light} 上 = 3.840 / 3.975 / 4.285 /
   4.010（四套全部不达标）。取 ${text} 与悬停态同键；描边同悬停取 ${accent_text}。 */
QPushButton#actionBtn:pressed {
    background: ${bg_light};
    border-color: ${accent_text};
    color: ${text};
}
QPushButton#actionBtn:disabled {
    background: ${disabled_bg};
    color: ${disabled_text};
    border-color: ${divider};
}
/* 10px 次级时间戳。取 ${text_hint}（vs chat_bg = 3.000 / 3.022 / 3.501 / 3.008）——
   低于小字 AA 的 4.5，但对照被替换掉的旧内联裸色 #BBBBBB（vs 白底仅 1.92）已是
   显著改善；进一步提对比需改色板（另一议题），故本轮**维持**此键并如实标注。 */
QLabel#timeLabel {
    font-size: 10px;
    color: ${text_hint};
    padding-left: 8px;
}
