# 码铃 上线前检查清单

> 生成时间：2026-09-14
> 代码基线：`HEAD=e705d32`，**所有修复在工作区、未提交**
> 工作副本：`D:\【试用测试】\maling_agent_改造\maling_agent_dev\`

---

## 一、结论速览

**✅ 可以上线。** 9 项修复全部经**未参与实现**的独立验证者逐条取证确认，**0 项证伪**；权威全量与独立复跑数字完全一致。

| 核验项 | 结果 | 由谁做 |
|---|---|---|
| 权威全量回归 | **1536 passed / 4 skipped / 0 failed**（40.90s） | team lead |
| 独立复跑全量 | **1536 passed / 4 skipped / 0 failed**（37.97s） | 独立验证者（数字一致 ✓） |
| 全树语法编译 | 503 个 `.py`，**0 失败** | team lead |
| 依赖清单零 diff（红线） | `requirements*.txt` / `pyproject.toml` 对基线与 HEAD **均 ZERO-DIFF** | 双方 |
| 7 个整文件硬保护 | **NONE 被碰** | 双方 |
| 段级保护：`gui/main_window.py` 的 24 个更新编排函数 + `_quit_stop_services` | **AST 逐行映射，NONE 命中**（改动只落 `_connect_signals` / `_setup_status_bar` / `_refresh_status_icons` + 3 个新增 home/session 函数） | 双方独立 |
| `gui/main.py` 编排链 | **零 diff** | 独立验证者 |
| v1.9 成果保护区 | `gui/fonts.py`、`assets/fonts/**` 零 diff；`theme_engine.py` 仅删 1 行未用变量 | 独立验证者 |
| 保护资产 | `gui/assets/fonts` / `maid` / `maid_pet` / **`roles`** / `maling.ico` **全部未改**（roles 已回滚，见 §六） | team lead |
| 新增 skip / xfail、测试被削弱 | **无** | 独立验证者 |

---

## 二、本轮修了什么（13 项）

### 🔴 缺陷
| # | 内容 | 证据 |
|---|---|---|
| 271 | **流式回复被裁剪**：`message_bubble` 快路径漏调高度重算 → 高度冻结在首个短分片 | 探针：short=45 → long=12305（修前恒 45） |
| 272 | **状态栏图标与文字互斥**：`QLabel` 只能显示其一，启动序列把三项各清一遍 | 三项现同时有文案+图标；图标不可用时文案完整、图标隐藏 |
| 273 | **构建期个人路径** + wheel 指向仓库外 | 删个人路径；重建产物 sha256 **未变** |
| 281 | **KB worker 应用退出崩溃** | 修前 `3221226505 = 0xC0000409`（Qt fail-fast），修后 rc=0 |
| 290 | **换肤丢失语义警示色**：`"state_warn" in cur` 拿色值串搜键名 → 恒假 | 修后 ui_night 下标签色 = `#e8b04c`（= 新主题 state_warn） |
| — | **「最近会话」点了没反应**：`session_selected` 零消费者，且**列表取的是"当前会话的消息"而非会话** | 修后点击 `active.id` 真的变化；点不存在的 id 不崩 |

### 🟡 一致性 / 卫生
| # | 内容 |
|---|---|
| 274 | 情绪弧线自建 1400ms 定时器绕过动效收口 → 迁 `motion.loop`；**呼吸观感按实测校准**（半周期 700→1599ms，回到原有 1400ms 节奏） |
| 275 | `except: pass` 收口（**只本轮新增 4 文件 / 14 处**；热路径"只记一次"，实测 20000 tick → 日志 1 条） |
| 278/291 | 矢量图标不随主题：`page_role` + 8 处（**6 处真缺陷，2 处经证伪零改动**） |
| 279 | 裸 emoji → 矢量字形（保留 emoji 回退）；`page_home` 标题缺 id 导致字号回落 + 换肤不刷新 |
| 277/280 | 4 处缺失字号（四主题 14→16，邻居零变化）+ **键盘焦点完全不可见**（`:focus` 被抹掉、编辑器回落系统蓝框） |
| 282 | `wheel_guard` 有 `set_enabled()` 却无配置键 → 补真实设置项（默认 True，升级用户行为不变） |
| 276 | 死代码与**伪口径**：`protect_qt_widget`（受控实验证明机制不需要）、`title_chain`（AST 证明 0 读取）、`__all__` 缺项（`QCursor` 曾说加了其实没加） |
| — | 鲸鱼娘资产：补回被误删的围裙/装饰/米饭，清残留白底（**用户明确要求**，见"授权例外"） |

### 本轮"证伪"项（值得记录：避免了无谓改动）
- `sidebar` / `chat_panel` 的图标**本来就会随主题变** → 零改动
- `emotion_arc` 的 1400ms 是"每半拍"不是"每周期" → 推翻了我的错误规格
- 我曾误判仓库有名为 `"docs` 的垃圾文件 → 实为 git 对中文名的八进制转义显示

---

## 三、⛔ 上线前**必须**处理

### 1. 🔴 `version.json` 未升版 → 自动更新会**静默失效**
现状：`version=2.1.1`、`release_url` 指向 `v2.1.1`、`released_at=2026-09-14`。
独立验证者读了改更检查逻辑确认：`evaluate_update` 仅在 `is_newer_version(远端, 本地)` 为真时才提示更新。
⇒ **若本次发新版不升 `version.json`，远端==本地 → 不提示、不自动更新，老用户永远收不到这批修复。**
**待你给目标版本号**（如 `2.1.2`），随后需同步：`version` / `released_at` / `release_url` / 重新打包生成的 `assets.*.sha256` 与 `size`。

### 2. 🟡 `.bak_*` 备份文件已**混进打包产物**
- 仓库内 `.bak_*` 共 **10 个**（gitignored，不进开源仓库）。
- ⚠️ 但 `dist_v2.1.1/maling/_internal/themes/base.qss.bak_glass_visibility` **已存在于发布包内**（打包规则含 `themes/*`）。
- 建议：① 在 `maid_coder_gui.spec` 增加 `*.bak*` 排除（防复发）② 之后再决定是否删除源树里的 `.bak_*`。
  （注意：这些 `.bak` 是"整文件重写留原文对照"的产物，而**那些重写尚未提交**，删掉会失去唯一"改动前"副本 → 建议**先提交、再删**。）

### 3. 🟡 若随对外分发，以下两项需修正
- `download.html`：含占位链接 `your-repo`、`your-mirror.example.com`，版本号写 **v1.0.0**（与 2.1.1 不符）→ 是坏链 + 过期页。
- `default.json`：旧 CLI 会话快照，其 system 提示词仍把角色**自称写成旧人设标签**（即 v1.9「人设 ≠ 人名」规则已清理的那类文案）并残留 `call_harness` 工具；被根 `main.py:122` 读取 → 提交进去会让新用户首启加载陈旧会话。
  > ⚠️ 本条**刻意不复述该旧文案原文**：源树有一条红线守卫（`tests/test_v19_batch1.py::TestTreeScan::test_no_self_maid_residue`）会全树扫描禁用串，复述会直接把守卫打红（这个坑我踩过一次，故留注脚）。

---

## 四、建议处理（非阻塞，但值得排期）

| 项 | 说明 |
|---|---|
| `page_help` 等 3 处标题**无 objectName** | 字号只能用「类名 + `>` 直接子」命中；日后若标题挪进内层容器会**静默失配回落 14px**。建议给这 3 个 QLabel 补 id（各 1 行） |
| `gui/assets/maid/_unused/` 5 个**假 PNG** | 实为 JPEG 却用 `.png` 后缀、**无 alpha**（必然白底），且被打进安装包；应删或转真 PNG |
| 鲸鱼娘图 **AIGC 标识** | 原图带 `AIGC` 元数据，转 PNG 时丢失。开源发布涉及 AI 生成内容标识合规 → 建议补回或 README 声明 |
| `docs/design-v21.md:781` | 含本机个人路径 `/c/Users/Administrator/...` |
| 文档伪口径 | 交接/设计文档中"已把 `QCursor` 加进 `__all__`"等陈述与代码不符（已改代码，文档待同步） |
| `emotion_arc` 构造期订阅无效 | 建页早于 `page_manager` 赋值 → 构造期那次订阅静默跳过；首次导航 `refresh()` 会重建并挂上 live 订阅，**生产可用**（仅构造期那次是死的） |
| 分区标题字重不统一 | 「本周回顾」700 vs 其余 400 |

---

## 五、已知残留 / 风险（不阻塞上线，但请知情）

1. **KB 任务退出时主线程有界阻塞至多 2.0s**：正常任务 ≤1.05s 无感；对话框关闭路径行为不变（仍即时 detach）。这是"协作式取消"的固有边界——`index_directory/search` 无取消钩子。
2. **视觉上有若干 px 级变化**（均为修复的必然结果，非观感改动）：
   - 4 处弹窗标题 14px → 16px（对齐同级标题）
   - 矢量字形比 emoji 每图标窄约 4px；**高度不变、无换行**
   - 焦点态新增 1px 描边（正是本次要修的"焦点不可见"）
   - 鲸鱼娘图：补回被误删的内部白色（围裙/装饰/米饭），清理残留白点/白边
3. **独立验证者的方法学边界**（如实转达）：它未在真实平台做视觉渲染（中文/字形可见性），B2/B5 用**桩图标**验证互斥与取色，非真字体渲染；未重建"24 个编排函数"的具体计数口径。

---

## 六、授权例外 —— **已撤销（本项目已无任何例外）**

原有一条例外：`gui/assets/roles/preset_whale/*.png`（37 张）曾被修改（补回被误删的围裙/衣料）。
**用户已指示回滚到"改动前那一版"**，现状态：

- `git status gui/assets/roles/preset_whale` → **干净**（与 HEAD 逐文件一致）
- 与"改动前备份" `_whale_png_backup_before_alpha_rebuild/` 比对 → **相同 37 / 不同 0**
- 已同步 `~/.maid_coder/role_assets/preset_whale/`（该目录优先级高于包内）

⇒ **美术资产现已真正"零改动"**，design-v21 §3.3 的资产红线**全部满足**，复核时无需任何例外说明。
依赖清单、字体资产、更新链同样未动。

---

## 七、已知内容限制（**不阻塞上线，但请知情**）

**鲸鱼娘 37 张图的背景里含"棋盘格"纹理，且它是源图自带的图像内容（不是透明化残留）。**

- 证据：背景区放大 3x 即为棋盘格（白格 ≈250 / 灰格 ≈237，周期 **≈20px**）；相邻的围裙是光滑白布。
- 成因：AI 出图时把"透明背景"**直接画成了棋盘格图案**，烤进了 RGB 像素，因此它是不透明的。
- **自动清除已尝试 6 种方法，全部否决**（最"好"的一版能把标记区残留降到 29%，但**目视把围裙、手、脸都吃穿了**；教训：质量验收不能只看指标，必须目视）。
- 若日后要处理，**建议在源图层面解决**：重新出图时要求纯色背景（纯白/绿幕）而非"透明背景"，或明确要求"不要绘制任何背景图案/棋盘格"。
- 影响面：头像在应用内为 32–128px，20px 棋盘格缩小后表现为**一圈浅灰雾**（深色主题下较可见）。
